trigger:
- main

pool:
  name: 'binet-window-agent'

steps:
- task: PowerShell@2
  displayName: '测试VM连接'
  inputs:
    targetType: 'inline'
    script: |
      # 测试基本网络连接
      Test-NetConnection -ComputerName 20.243.10.36 -Port 445
      Write-Host "如果上面显示TcpTestSucceeded: True，说明网络连通"